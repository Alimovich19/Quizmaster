services:
  - type: web
    name: quiz-backend
    env: python
    plan: free
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    rootDir: .
    pythonVersion: 3.10